# Omarchy environment (OMARCHY_PATH + PATH), needed even for non-interactive shells
[[ -r /usr/share/omarchy/default/bash/env-bootstrap ]] && source /usr/share/omarchy/default/bash/env-bootstrap

# User-owned Omarchy command overrides (including the Evangelion screensaver)
# must resolve before the packaged /usr/bin commands in login and idle shells.
export PATH="$HOME/.local/bin:$PATH"

# If not running interactively, don't do anything else (leave this above the rc source)
[[ $- != *i* ]] && return

# All the default Omarchy aliases and functions
# (don't mess with these directly, just overwrite them here!)
source "$OMARCHY_PATH/default/bash/rc"

# EVA terminal and TUI profile integration. This file is generated by
# eva-terminal-profile and keeps all tool-specific overrides reversible.
eva_tools_env="${XDG_STATE_HOME:-$HOME/.local/state}/evangelion-rice/tools/tools.env"
[[ -r $eva_tools_env ]] && source "$eva_tools_env"
unset eva_tools_env

# Notify on long-running interactive commands without retaining command text.
[[ -r $HOME/.config/omarchy/magi-command-telemetry.bash ]] && source "$HOME/.config/omarchy/magi-command-telemetry.bash"

# Add your own exports, aliases, and functions here.
#
# Make an alias for invoking commands you use constantly
# alias p='python'
